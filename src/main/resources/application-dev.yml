config-repo-path: file:///Users/vladimir.perfilev/Projects/MyProjects/FaToDo/fatodo-config-repo/

spring:
  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: ${config-repo-path}, ${config-repo-path}{application}
